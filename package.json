{
  "name": "hns-id",
  "version": "0.2.0",
  "repository": "https://github.com/namebasehq/hns-id-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ hns-id",
    "create-local": "graph create --node http://localhost:8020/ hns-id",
    "remove-local": "graph remove --node http://localhost:8020/ hns-id",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hns-id",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.51.2",
    "@graphprotocol/graph-ts": "^0.31.0",
    "assemblyscript": "^0.19.0",
    "matchstick-as": "^0.5.2",
    "typescript": "^4.9.4"
  }
}
